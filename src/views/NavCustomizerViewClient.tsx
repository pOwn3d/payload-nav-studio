'use client'

// Import via relative path — this file is built as a separate "use client" entry
// so that dnd-kit (bundled inside NavCustomizer) stays within the client boundary
export { NavCustomizer as NavCustomizerViewClient } from '../components/NavCustomizer.js'
